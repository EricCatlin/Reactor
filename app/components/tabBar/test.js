import * as actions from './actions'
import reducer from './reducers'

describe('Test Group', () => {
  it('Test Single', () => {

    //Set up logic, 
    //Test outcome
    //expect(actions.hire(1)).not.toEqual(null)
    
  });
})

